# Fixture: three reviewed findings against a queue Noru returned. The validator must exit 0.
#
# What each one is here to show:
#   * the credential finding carries a risk the organization already holds, and the reviewer both
#     confirmed the rule and added the fact the rule cannot see — that the value needs rotating.
#   * the ingress finding is attached to an asset that is already in the register. This piece never
#     creates an asset, so the only ones it may name are the ones the queue snapshot returned.
#   * the unpinned-action finding is ACCEPTED at a lower severity than the rule proposed, with the
#     reasoning written out. That is the case the validator is strictest about: a decision to leave
#     a misconfiguration in place is the one most likely to be a shrug.
#
# The second entry in open_findings has no matching finding below, so :diff will plan to close it
# with the same keyed upsert. The validator warns about that rather than failing: closing a finding
# whose rule stopped firing is the point of re-running.
#
# No id here belongs to any organization. Nothing in this file is a matched line: this piece records
# citations, never copies, because one of its rules fires on a line that holds a credential.
version: 0.1.0
piece: iac-scan
source:
  slug: example-org/example-app
  commit_sha: 4f3c1a9e77b2d5c8a10e6b4f2d9c3a71e5b80d64
  branch: main
  generated_by: iac-scan@0.1.0
queue_snapshot:
  fetched_at: 2026-08-27T09:14:00Z
  via:
    - getSecurityFindings
    - getOrganizationAssets
    - getOrganizationRisks
  source: iac-scan
  open_findings:
    - external_id: "example-org/example-app:terraform-credential-literal-in-source.a1b2c3d4e5f6"
      check_name: terraform-credential-literal-in-source
      title: Credential appears to be written into the configuration
      severity: critical
      status: open
      category: identity_access
    - external_id: "example-org/example-app:kubernetes-container-runs-privileged.f0e1d2c3b4a5"
      check_name: kubernetes-container-runs-privileged
      title: Container requests privileged mode
      severity: critical
      status: open
      category: configuration
  assets:
    - id: EXAMPLE-ASSET-ID
      external_id: example-account/example-cluster
      name: Example application cluster
  risks:
    - id: EXAMPLE-RISK-ID
      title: "Example risk: internet-reachable infrastructure"
findings:
  - key: terraform-credential-literal-in-source.a1b2c3d4e5f6
    check: terraform-credential-literal-in-source
    technology: terraform
    severity: critical
    category: identity_access
    status: in_progress
    title: Credential appears to be written into the configuration
    file: infra/storage.tf
    resource: aws_db_instance.primary
    observed_on: 2026-08-20
    refs:
      - infra/storage.tf:13
    risk_id: EXAMPLE-RISK-ID
    owner_email: sam.okafor@example.com
    interpretation:
      owner: sam.okafor@example.com
      decided_at: 2026-08-24
      expires_at: 2026-10-15
      rationale: >
        I opened the cited line. It is a real literal and not a variable reference, so the value is
        in the history and has to be rotated as well as moved. Filed against the existing risk while
        the rotation is in flight.
  - key: terraform-network-ingress-open-to-the-internet.0f9e8d7c6b5a
    check: terraform-network-ingress-open-to-the-internet
    technology: terraform
    severity: high
    category: configuration
    status: open
    title: Network ingress rule accepts traffic from any address
    file: infra/network.tf
    resource: aws_security_group.web
    observed_on: 2026-08-20
    refs:
      - infra/network.tf:10
    asset_external_id: example-account/example-cluster
    interpretation:
      owner: priya.nair@example.com
      decided_at: 2026-08-24
      expires_at: 2026-10-15
      rationale: >
        The port is genuinely public, but the rule sits in front of the cluster the register already
        knows about, so the finding is attached to that asset rather than to a new one.
  - key: github-actions-third-party-action-unpinned.9a8b7c6d5e4f
    check: github-actions-third-party-action-unpinned
    technology: github_actions
    severity: low
    category: vulnerability
    status: accepted
    title: Workflow step uses an action that is not pinned to a commit
    file: .github/workflows/release.yml
    resource: publish
    observed_on: 2026-08-20
    refs:
      - .github/workflows/release.yml:11
    owner_email: priya.nair@example.com
    interpretation:
      owner: priya.nair@example.com
      decided_at: 2026-08-24
      expires_at: 2027-06-30
      rationale: >
        Downgraded from the rule's default: this workflow publishes nothing and holds no secret, so
        a mutable reference here changes what runs but cannot reach anything. Accepted until the
        next pipeline review, at which point the pin should land with the rest of them.
